import React from "react";
import CurrentTableComponent from "./currentTable.component";

const CurrentTable = ({ listSettings }) => {
  return <CurrentTableComponent listSettings={listSettings} />;
};

export default CurrentTable;
